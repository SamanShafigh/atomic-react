import { css } from 'glamor'

export const getPostStyle = () => {
  return css({
    margin: '0.3em',
  });
}

export const getPostFooterStyle = () => {
  return css({
    color: '#ccc',
    paddingLeft: '5px',
    paddingRight: '15px',
  });
}